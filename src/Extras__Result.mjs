// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Primitive_option from "rescript/lib/es6/Primitive_option.js";
import * as Primitive_exceptions from "rescript/lib/es6/Primitive_exceptions.js";

function fromArrayMap(xs, f) {
  let oks = [];
  let _inx = 0;
  while (true) {
    let inx = _inx;
    let x = xs[inx];
    if (x === undefined) {
      return {
        TAG: "Ok",
        _0: oks
      };
    }
    let ok = f(Primitive_option.valFromOption(x));
    if (ok.TAG !== "Ok") {
      return ok;
    }
    oks.push(ok._0);
    _inx = inx + 1 | 0;
    continue;
  };
}

function fromArray(xs) {
  return fromArrayMap(xs, i => i);
}

function mapError(r, f) {
  if (r.TAG === "Ok") {
    return r;
  } else {
    return {
      TAG: "Error",
      _0: f(r._0)
    };
  }
}

function fromTryCatch(f) {
  let m = i => i;
  try {
    let ok = f();
    return {
      TAG: "Ok",
      _0: ok
    };
  } catch (raw_e) {
    let e = Primitive_exceptions.internalToException(raw_e);
    return {
      TAG: "Error",
      _0: m(e)
    };
  }
}

function toOption(r) {
  if (r.TAG === "Ok") {
    return Primitive_option.some(r._0);
  }
  
}

function getError(r) {
  if (r.TAG === "Ok") {
    return;
  } else {
    return Primitive_option.some(r._0);
  }
}

export {
  fromArrayMap,
  fromArray,
  mapError,
  fromTryCatch,
  toOption,
  getError,
}
/* No side effect */
